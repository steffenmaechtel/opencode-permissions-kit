name: tests

# Trigger only once on push to master (plus manual). Avoids the classic
# double-run: a push to a branch with an open PR fires both `push` and
# `pull_request`, and a tag push fires `push` again.
on:
  push:
    branches:
      - master
  workflow_dispatch:

# Cancel a superseded run when a new commit lands on the same ref; also
# dedupes if two events somehow fire for the same push.
concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Check version consistency
        run: make check-version
      - name: Make scripts executable
        run: chmod +x ./tests/test-wrapper-validation.sh ./tests/test-project-config.sh ./tests/test-hooks.sh ./tests/test-jsonc-parser.sh ./tests/test-git-config.sh ./tests/test-ddev-shim.sh ./tests/test-container-backend.sh ./tests/test-bypass-guard.sh ./files/opencode-permissions-kit-lib/hooks/post-checkout ./files/opencode-permissions-kit-lib/hooks/post-merge ./files/opencode-permissions-kit-lib/hooks/post-commit ./files/config.sh ./files/update.sh ./files/install.sh ./files/uninstall.sh ./files/status.sh ./files/opencode-permissions-kit-lib/protect-projects.sh ./files/opencode-permissions-kit-lib/wrapper ./files/opencode-permissions-kit-lib/log.sh ./files/opencode-permissions-kit-lib/shell-warn.sh ./files/opencode-permissions-kit-lib/bin/ddev
      - name: Run wrapper validation tests
        run: ./tests/test-wrapper-validation.sh
      - name: Run project config tests
        run: ./tests/test-project-config.sh
      - name: Run git hook regression tests
        run: ./tests/test-hooks.sh
      - name: Run JSONC parser edge-case tests
        run: ./tests/test-jsonc-parser.sh
      - name: Run git-config toggle tests
        run: ./tests/test-git-config.sh
      - name: Run ddev shim tests
        run: ./tests/test-ddev-shim.sh
      - name: Run container backend tests
        run: ./tests/test-container-backend.sh
      - name: Run wrapper-bypass guard tests
        run: ./tests/test-bypass-guard.sh
